# Privacy Auto-Anonymizer
**هدف:** سامانه تعاملی پالایش دادههای حساس (چهره، پلاک، متن)
**پشته فناوری:** Python 3.10+, Django, YOLOv8/v9, EasyOCR, HTML5 Canvas, Tailwind CSS (Dark Minimalist)

## معماری و جریان داده
۱. کلاینت تصویر را آپلود میکند.
۲. سرور با YOLO و EasyOCR مختصات عناصر حساس (BBox) را استخراج کرده و به صورت JSON (بدون دستکاری پیکسلها) برمیگرداند.
۳. فرانتاند روی HTML5 Canvas تصویر و کادرهای پالسدار (آبی برای چهره، سبز برای پلاک، زرد برای متن) را رسم میکند.
۴. کاربر با کلیک روی هر کادر (Human-in-the-Loop) آن ناحیه را فعال/غیرفعال میکند (ماسک شطرنجی+نویز برای عکس و کادر مشکی برای متن).
۵. خروجی نهایی مستقیم از Canvas مرورگر دانلود میشود.

## نقشه راه ۷ روزه
- [x] روز اول: ساختار پایه جنگو و رابط کاربری خام (Dark Minimalist).
- [x] روز دوم: ادغام YOLO و EasyOCR در بکاند و استخراج JSON مختصات.
- [ ] روز سوم: پیادهسازی فیلترهای امنیتی با OpenCV و NumPy (DP-Pix و Solid Black).
- [ ] روز چهارم: ساخت REST API برای تبادل مختصات بین جنگو و فرانتاند.
- [ ] روز پنجم: رندر تصویر و کادرهای رنگی روی HTML5 Canvas با JS خالص.
- [ ] روز ششم: منطق تعاملی کلیک روی کادرها (فعال/غیرفعالسازی ماسک).
- [ ] روز هفتم: تکمیل دکمهها (انتخاب همه/لغو همه) و استخراج تصویر نهایی از کلاینت.

## قوانین مهم توسعه
۱. پس از پایان هر تسک، فایل PROJECT_MEMORY.md را بازنگری کن و تیک انجام آن مرحله را در نقشه راه بزن تا وضعیت پیشرفت پروژه همواره بهروز بماند.
۲. پیام کامیت‌های گیت (Commit Messages) باید بسیار کوتاه، خلاصه، طبیعی و به سبک برنامه‌نویس انسانی باشد (بدون ذکر "روز فلان"، بدون قالب‌های طولانی یا لحن هوش مصنوعی؛ کاملاً مختصر و با حروف کوچک مثل `init project & ui`، `add detector`، `fix canvas bbox`).
۳. همواره برای نگارش داک‌استرینگ‌ها (Docstrings) و منطق ریاضی فیلترها در `filters.py` به فایل `RESEARCH_PAPERS.md` رجوع شود.
